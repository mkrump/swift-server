import Foundation

class Server {
//    TODO Add contstructor
    func start(args: [String: String]) {
        for (arg, value) in args {
            print("'\(arg)': '\(value)'")
        }
        while true {
        }
    }
}
